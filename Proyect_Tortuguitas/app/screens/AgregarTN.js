import React, { useState, useEffect, useRef } from "react";
import {
  StyleSheet,
  View,
  Text,
  FlatList,
  ActivityIndicator,
  TouchableOpacity,
  Alert
} from "react-native";
import { Image, Icon, Button } from "react-native-elements";

export default function AgregarTN(props) {
  return (
    <View>
      <Text size={24}> En breve se desarrollará esta opción</Text>
    </View>
  );
}
